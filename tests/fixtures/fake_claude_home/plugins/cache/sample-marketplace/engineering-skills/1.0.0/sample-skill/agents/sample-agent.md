---
name: sample-agent
description: A tiny sample agent for fixture purposes.
---

# Sample Agent

Fake agent for tests. Same role as the fake skill — it just needs to exist
on disk under an `agents/` directory so the scanner can find it.
